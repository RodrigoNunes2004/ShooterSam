# ShooterSam

Unreal Engine project. This repository tracks only the game source code.

## Contents
- `Source/` C++ game code

## Requirements
- Unreal Engine (version used by the project)
- Visual Studio with C++ tools

## Build
Open `ShooterSam.uproject` in Unreal Engine and build the project from the editor.
